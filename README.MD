can i overwrite readme in branch??

yessir i can!

let's try a sub-branch

---

q'$:

  can branches be made private (forked repo can't unless using unbind from fork network feat. ig)

  can colleagues be assigned to just a branch w/o seeing the repo @all? (not on public like this obviously but if it was private??)

  if not: manually link instead of using fork link after using unbind? then just do another fork &give that away as branch   for collab?
